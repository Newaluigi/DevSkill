export class Skill {
  name: string;
  logoUrl: string;
  site: string;

  constructor(name: string, logoUrl: string, site: string) {
    this.name = name;
    this.logoUrl = logoUrl;
    this.site = site;
  }
}
